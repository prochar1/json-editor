export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Editor dat",
  description: "Upravte data a uložte změny snadno a rychle.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ],
};
